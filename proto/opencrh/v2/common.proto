syntax = "proto3";

package opencrh.v2;

// Shared error and quota metadata used by every v2 operation response.

message ApiError {
  string code = 1;
  string message = 2;
}

message ApiMeta {
  int64 remain = 1;
  int64 cost = 2;
  optional int64 retry_after = 3;
}
